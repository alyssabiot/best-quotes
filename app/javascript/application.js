// Import Rails UJS (activates Rails forms, data-remote, etc.)
import Rails from "@rails/ujs";
Rails.start();

// Import Turbo (if using Turbo)
import { Turbo } from "@hotwired/turbo-rails";

// Import styles if using JS bundler to compile SCSS
// import "../stylesheets/application.scss";

// Import Bootstrap JS
import "bootstrap";